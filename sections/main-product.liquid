<section class="premium-product fade-in">

  <div class="premium-container">

    <div class="premium-gallery">
      <div class="premium-main-image">
        <img 
          id="MainProductImage"
          src="{{ product.featured_image | image_url: width: 1400 }}"
          alt="{{ product.title }}">
      </div>

      <div class="premium-thumbnails">
        {% for image in product.images %}
          <img
            src="{{ image | image_url: width: 300 }}"
            onclick="document.getElementById('MainProductImage').src='{{ image | image_url: width: 1400 }}'"
          >
        {% endfor %}
      </div>
    </div>

    <div class="premium-info">

      <h1 class="premium-title">{{ product.title }}</h1>

      <div class="premium-price-box">
        <span class="premium-price">
          {{ product.price | money }}
        </span>
      </div>

      <div class="premium-description">
        {{ product.description }}
      </div>

      <form method="post" action="/cart/add" id="product-form">
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">

        <div class="premium-quantity">
          <label>Quantity</label>
          <input type="number" name="quantity" value="1" min="1">
        </div>

        <button type="submit" class="premium-add">
          Add to Cart
        </button>
      </form>

    </div>

  </div>

</section>

<!-- Sticky Add to Cart -->
<div class="sticky-cart">
  <div class="sticky-inner">
    <span>{{ product.title }} — {{ product.price | money }}</span>
    <button onclick="document.getElementById('product-form').submit()">
      Add to Cart
    </button>
  </div>
</div>
